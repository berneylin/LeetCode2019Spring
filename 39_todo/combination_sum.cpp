#include<vector>
using namespace std;

class Solution {
public:
    vector<vector<int>> combinationSum(vector<int>& candidates, int target) {
        int subsetsNum = 1>>candidates.size();  // the num of subsets is 2^candidates.size()
        for(int i=0;i<subsetsNum;i++){
            
        }
    }
};